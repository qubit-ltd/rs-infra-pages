name: Publish rs-infra-pages site
description: Upload and deploy a site generated by rs-infra-pages
runs:
  using: composite
  steps:
    - uses: actions/upload-pages-artifact@v3
      with:
        path: public
    - uses: actions/deploy-pages@v4
